#!/bin/bash
# Strip non-numeric characters from the clipboard

set -euo pipefail
LIB="$(dirname "$(readlink -f "$0")")/lib"

RESULT="$(/usr/bin/pbpaste | sed 's/[^0-9]*//g')"
printf '%s' "$RESULT" | /usr/bin/pbcopy
"$LIB/notify" --from "$(basename "$0")" -- "$RESULT"
