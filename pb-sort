#!/bin/sh

if [ $(pbpaste | wc -l) -gt 1 ]; then
  pbpaste | sort -fn | pbcopy;
else
  pbpaste | tr , "\n" | sed 's/^ //' | sort -fn | paste -s -d, - | pbcopy
fi
/usr/bin/env growlnotify -a Quicksilver -t "Sorted" -m "`pbpaste`"
