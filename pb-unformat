#!/bin/bash
# Strip rich-text formatting from the clipboard, leaving plain text

set -euo pipefail
LIB="$(dirname "$(readlink -f "$0")")/lib"

RESULT="$(/usr/bin/pbpaste)"
printf '%s' "$RESULT" | /usr/bin/pbcopy
"$LIB/notify" --from "$(basename "$0")" -- "$RESULT"
